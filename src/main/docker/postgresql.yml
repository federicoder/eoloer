version: '2'
services:
  eolo-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/eolo/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=eolo
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
